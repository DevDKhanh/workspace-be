enum ROUTER {
  HOME = "/",
  LOGIN = "/login",
  PROJECTS = "/projects",
  MEMBERS = "/members",
  TEAM = "/team",
  POSITIONS = "/positions",
  PROJECT_ACTIONS = "/project-actions",
  WORKS = "/works",
}

export default ROUTER;
